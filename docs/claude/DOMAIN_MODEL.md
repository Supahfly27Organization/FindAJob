# Domain Model

<!-- Document every entity/aggregate here as the schema takes shape: primary key, fields
     with types and DB column notes, relationships (FK), unique indexes, and any
     non-obvious business rules or invariants. This is the single source of truth for
     "what does the data look like" — keep it in sync with migrations as the schema
     evolves. Delete this comment once real entities are documented below. -->

## <EntityName>

PK: `<field>` (`<type>`). <One-line description of what this entity represents.>

| Property | Type | DB | Notes |
|---|---|---|---|
| | | | |

<!-- Repeat one `##` section per entity/aggregate root. Call out owned/value types,
     enums, and junction tables where they matter to correctness. -->
